<Project>

  <PropertyGroup>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>

    <!-- NuGet properties -->
    <PackageVersion>0.1.0</PackageVersion>

  </PropertyGroup>

</Project>